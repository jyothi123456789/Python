stack = []
#append () function to push
#element in the stack
stack.append(a)
stack.append(b)
stack.append(c)
print('initial stack')
print(stack)


#pop() function to pop
#eliment from stack in
#lifo order
print ('\n stack after elements are popped:')


       )